const Extract = {
  getFiles(platform, token) {
    // @TODO
    // this.getFileNames(platform, token)
    // this.getFileContents(platform, token)
  },
  getFileNames(platform, token) {
    // @TODO
  },
  getFileContents(platform, token) { // Singular file
    // @TODO
  }
}

export default Extract
